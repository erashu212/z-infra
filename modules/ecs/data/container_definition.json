{
    "name": "${name}",
    "imageUrl": "${imageUrl}",
    "cpu": "${cpu}",
    "memory": "${memory}",
    "essential": "${essential}",
    "portMappings": "${portMappings}",
    "volume": "${volume}"
  }